# postgrest-sample

## 実行方法
1. 本PJをvscodeで開き、コンテナデバッグを起動する
1. test.httpを開く
1. 上から順番にSend Requestを押下する
    - ※Send Requestが見つからない場合はVSCodeの RestClient Extentionがリモートコンテナにインストールされていないため、リモートコンテナに手動でインストールしてください。通常は自動で入るように仕組んでいます。


